function ProjectCard({ title, description, techniques }) {
  return (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      {/*techniques-component + knappar till netlify och github */}
    </article>
  );
}

export default ProjectCard;
